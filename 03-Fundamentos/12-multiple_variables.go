package main

/*
var a, b, c int // Variables del mismo tipo

var name, city = "Miguel", "Puebla"

var x, y = 10, 3.1415

// Usarlo para bloques de variables relacionadas, para no repetir var, que sean para algun config o declaration de variables globales,
// ya que no se puede usar fuera de una función
var (
	age      int    = 30
	isActive bool   = true
	nickname string = "MiguelGo"
)

func multiple_variables() {
	c, d := 10, 20

	//Usar dentro de una función para declarar variables locales, ya que no se puede usar fuera de una función
	full_name, ok := "Miguel Medina", true

	c, d, e := 20, 30, 10
}
*/
